# Shell, I/O Redirection and Filters

This project covers Linux I/O redirections, pipelines, special characters, and essential text-filtering commands (`cd`, `ls`, `head`, `tail`, `wc`, `grep`, etc.).

## Scripts Description

* `0-hello_world`: A shell script that prints "Hello, World" followed by a new line to the standard output.
